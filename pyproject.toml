[tool.poetry]
name = "backend-django"
version = "0.1.0"
description = ""
authors = ["natawgxy"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
